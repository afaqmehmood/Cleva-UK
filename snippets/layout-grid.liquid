<div
  class="
    h-full pointer-events-none
    hidden target:block
    fixed inset-0 z-[999]
    container
  "
  id="layout-grid"
>
  <div class="h-full main-grid">
    {% for i in (1..12) %}
      <div class="h-full bg-[#FF0000]/10"></div>
    {% endfor %}
  </div>
</div>
